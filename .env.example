# Copy this file to .env and fill in your key. Never commit .env.
# The key is read server-side only (server/index.ts) — it is never sent to the browser.
ANTHROPIC_API_KEY=

# Optional: port for the Express server (defaults to 8787)
PORT=8787
