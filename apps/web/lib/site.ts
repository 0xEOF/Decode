// The single source of truth for the product name shown across the site
// (nav, hero, metadata, legal pages, etc.) — change it here, not in place.
// See app/layout.tsx for the matching SITE_URL, and README.md "SEO" for how
// to update the domain everywhere at once if it ever changes.
export const SITE_NAME = 'Reveala';
export const CONTACT_EMAIL = 'privacy@reveala.app';
