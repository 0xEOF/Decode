// The single source of truth for the product name shown across the site
// (nav, hero, metadata, legal pages, etc.) — change it here, not in place.
// See app/layout.tsx for the matching SITE_URL placeholder note — both get
// swapped together, see README.md "SEO".
export const SITE_NAME = 'Reveala';
export const CONTACT_EMAIL = 'privacy@REPLACE-WITH-YOUR-DOMAIN.com';
